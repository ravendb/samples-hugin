[Unit]
# Let Ollama page the embedding model in before RavenDB maps the database.
After=ollama.service
Wants=ollama.service

[Service]
# A HuginAI Voron flush exceeded the package default on Pi Zero 2 W. A forced
# SIGKILL here can invalidate the database artifact used for the next delta.
TimeoutStopSec=300
